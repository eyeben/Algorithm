-- 코드를 입력하세요

SELECT
    CATEGORY,
    SUM(SALES) AS TOTAL_SALES
FROM
(SELECT
    *
FROM BOOK_SALES 
WHERE SALES_DATE LIKE '2022-01%') S
JOIN BOOK B USING (BOOK_ID)
GROUP BY
    CATEGORY
ORDER BY 1 ASC
;